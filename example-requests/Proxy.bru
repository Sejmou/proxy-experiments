meta {
  name: Proxy
  type: http
  seq: 3
}

get {
  url: http://localhost
  body: none
  auth: none
}

docs {
  Accesses the nginx server (or, to be more precise, the OpenResty server, i.e. basically nginx w/ Lua language support for possibly more sophisticated logic) exposed on port 80 (default port for HTTP) from a Docker Compose multi-container application setup running on localhost.
}
