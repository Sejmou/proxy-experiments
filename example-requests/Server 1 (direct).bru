meta {
  name: Server 1 (direct)
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/what-is-my-ip
  body: none
  auth: none
}

docs {
  Tries to get IP address directly from API server 1. The server is accessed via the port exposed to localhost from a local Docker Compose multi-container application.
}
